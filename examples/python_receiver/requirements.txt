rns>=0.9.0
# No other specific dependencies for this basic script,
# but 'pyserial' might be needed by RNS for some interface types
# (though not directly by this receiver script if RNS handles interfaces).
